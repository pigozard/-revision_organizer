<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fiches de Révision - Rails + IA | RevisionOrganizer</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
    <style>
        :root {
            --dark-bg: #0a0e27;
            --dark-card: #151a30;
            --accent-blue: #3b82f6;
            --accent-cyan: #06b6d4;
            --accent-purple: #8b5cf6;
            --neon-blue: #60a5fa;
            --text-light: #e5e7eb;
            --text-muted: #9ca3af;
            --border-color: rgba(59, 130, 246, 0.3);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #0a0e27 0%, #1a1f3a 50%, #0a0e27 100%);
            background-attachment: fixed;
            color: var(--text-light);
            line-height: 1.6;
            min-height: 100vh;
        }

        /* Header */
        header {
            background: linear-gradient(135deg, #3b82f6 0%, #06b6d4 100%);
            padding: 4rem 2rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.1'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
            opacity: 0.3;
        }

        header h1 {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            position: relative;
            text-shadow: 0 2px 10px rgba(0,0,0,0.3);
        }

        header p {
            font-size: 1.2rem;
            opacity: 0.95;
            position: relative;
            margin-bottom: 1rem;
        }

        .badge {
            display: inline-block;
            background: rgba(255,255,255,0.2);
            padding: 0.6rem 1.5rem;
            border-radius: 25px;
            font-size: 0.9rem;
            margin-top: 1rem;
            backdrop-filter: blur(10px);
            position: relative;
            font-weight: 600;
        }

        /* Navigation sticky */
        nav {
            background: rgba(21, 26, 48, 0.9);
            backdrop-filter: blur(10px);
            padding: 1.2rem 2rem;
            position: sticky;
            top: 0;
            z-index: 100;
            border-bottom: 1px solid var(--border-color);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);

        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 0.8rem;
            max-width: 1400px;
            margin: 0 auto;
            flex-wrap: wrap;
            justify-content: center;
        }

        nav a {
    color: #9ca3af;
    text-decoration: none;
    padding: 0.8rem 1.2rem;
    border-radius: 0;
    font-size: 0.9rem;
    font-weight: 500;
    transition: all 0.3s ease;
    white-space: nowrap;
    border-bottom: 2px solid transparent;
    position: relative;
}

      nav a:hover {
          color: #60a5fa;
          border-bottom-color: #3b82f6;
      }

      nav a::after {
          content: '';
          position: absolute;
          bottom: -2px;
          left: 0;
          width: 0;
          height: 2px;
          background: linear-gradient(90deg, #3b82f6, #06b6d4);
          transition: width 0.3s ease;
      }

      nav a:hover::after {
          width: 100%;
      }

        /* Main Content */
        main {
            max-width: 1200px;
            margin: 0 auto;
            padding: 3rem 2rem;
        }

        /* Section Titles */
        .section-title {
            font-size: 2rem;
            font-weight: 700;
            margin: 4rem 0 2rem 0;
            padding-bottom: 1rem;
            border-bottom: 2px solid var(--border-color);
            display: flex;
            align-items: center;
            gap: 1rem;
            background: linear-gradient(135deg, #60a5fa 0%, #06b6d4 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .section-title .icon {
            font-size: 2.2rem;
            background: none;
            -webkit-text-fill-color: initial;
        }

        /* Cards modernes */
        .card {
            background: rgba(21, 26, 48, 0.8);
            backdrop-filter: blur(10px);
            border-radius: 16px;
            padding: 2.5rem;
            margin-bottom: 3rem;
            border: 1px solid var(--border-color);
            position: relative;
            overflow: hidden;
            animation: fadeInUp 0.6s ease;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
            transition: all 0.3s;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 40px rgba(59, 130, 246, 0.2);
            border-color: rgba(59, 130, 246, 0.5);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #3b82f6, #06b6d4);
        }

        .card h2 {
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            color: var(--text-light);
        }

        .card h2 .emoji {
            font-size: 2rem;
        }

        .card h3 {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--neon-blue);
            margin: 2rem 0 1rem 0;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Tags */
        .tags {
            display: flex;
            gap: 0.6rem;
            margin-bottom: 1.5rem;
            flex-wrap: wrap;
        }

        .tag {
            font-size: 0.75rem;
            padding: 0.4rem 0.8rem;
            border-radius: 6px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .tag.rails { background: linear-gradient(135deg, #e63946, #f72585); color: white; }
        .tag.ruby { background: linear-gradient(135deg, #cc342d, #e63946); color: white; }
        .tag.ai { background: linear-gradient(135deg, #10a37f, #2ecc71); color: white; }
        .tag.js { background: #f7df1e; color: black; }
        .tag.git { background: #f05032; color: white; }
        .tag.ux { background: linear-gradient(135deg, #ff69b4, #e91e63); color: white; }

        /* Lists */
        .card ul {
            list-style: none;
        }

        .card li {
            padding: 0.7rem 0;
            padding-left: 2rem;
            position: relative;
            color: var(--text-muted);
            line-height: 1.7;
        }

        .card li::before {
            content: '→';
            position: absolute;
            left: 0;
            color: var(--accent-cyan);
            font-weight: bold;
            font-size: 1.2rem;
        }

        .card li strong {
            color: var(--text-light);
        }

        /* Error lists */
        .errors {
            background: rgba(230, 126, 34, 0.1);
            border: 1px solid rgba(230, 126, 34, 0.3);
            border-radius: 12px;
            padding: 1.5rem;
            margin: 1rem 0;
        }

        .errors li::before {
            content: '⚠';
            color: #e67e22;
        }

        /* Tables */
        .table-wrapper {
            overflow-x: auto;
            margin: 1.5rem 0;
            border-radius: 12px;
            border: 1px solid var(--border-color);
            background: rgba(10, 14, 39, 0.6);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.9rem;
        }

        th {
            background: rgba(59, 130, 246, 0.1);
            color: var(--neon-blue);
            padding: 1rem 1.5rem;
            text-align: left;
            font-weight: 600;
            border-bottom: 2px solid var(--border-color);
        }

        td {
            padding: 1rem 1.5rem;
            border-bottom: 1px solid rgba(59, 130, 246, 0.1);
            color: var(--text-muted);
        }

        tr:last-child td {
            border-bottom: none;
        }

        tr:hover td {
            background: rgba(59, 130, 246, 0.05);
        }

        /* Code */
        code {
            font-family: 'JetBrains Mono', 'Fira Code', monospace;
            background: rgba(10, 14, 39, 0.8);
            padding: 0.3rem 0.6rem;
            border-radius: 6px;
            font-size: 0.85rem;
            color: var(--accent-cyan);
            border: 1px solid rgba(59, 130, 246, 0.2);
        }

        pre {
            background: rgba(10, 14, 39, 0.9);
            border-radius: 12px;
            padding: 1.5rem;
            overflow-x: auto;
            margin: 1.5rem 0;
            border: 1px solid var(--border-color);
        }

        pre code {
            background: none;
            padding: 0;
            font-size: 0.85rem;
            line-height: 1.8;
            color: var(--text-muted);
            border: none;
        }

        /* Syntax highlighting */
        .keyword { color: #ff79c6; }
        .string { color: #f1fa8c; }
        .method { color: #50fa7b; }
        .class { color: #8be9fd; }
        .comment { color: #6272a4; font-style: italic; }
        .symbol { color: #bd93f9; }
        .number { color: #ffb86c; }
        .variable { color: #f8f8f2; }

        /* Footer */
        footer {
            text-align: center;
            padding: 3rem 2rem;
            color: var(--text-muted);
            font-size: 0.9rem;
            border-top: 1px solid var(--border-color);
            margin-top: 4rem;
        }

        footer strong {
            color: var(--neon-blue);
        }

        /* Responsive */
        @media (max-width: 768px) {
            header h1 {
                font-size: 2rem;
            }

            nav ul {
                justify-content: flex-start;
            }

            main {
                padding: 1.5rem;
            }

            .card {
                padding: 1.5rem;
            }

            .section-title {
                font-size: 1.5rem;
            }
        }

        /* Scroll to top button */
        .scroll-to-top {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: linear-gradient(135deg, #3b82f6 0%, #06b6d4 100%);
            color: white;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            opacity: 0;
            transition: all 0.3s;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.4);
            z-index: 999;
        }

        .scroll-to-top.visible {
            opacity: 1;
        }

        .scroll-to-top:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 25px rgba(59, 130, 246, 0.6);
        }
    </style>
</head>
<body>
<img src="" alt="">
<header>
    <h1> 🚂 Fiches de Révision Rails + IA</h1>
    <p>Le Wagon - AI Software & Full-Stack Development</p>
    <span class="badge">  13 Fiches Complètes</span>
</header>

<nav>
    <ul>
        <li><a href="#active-record">Active Record</a></li>
        <li><a href="#migrations">Migrations</a></li>
        <li><a href="#associations">Associations</a></li>
        <li><a href="#validations">Validations</a></li>
        <li><a href="#llm-basics">LLM Basics</a></li>
        <li><a href="#prompt-engineering">Prompt Engineering</a></li>
        <li><a href="#chat-models">Chat Models</a></li>
        <li><a href="#tools-agents">Tools & Agents</a></li>
        <li><a href="#streaming">Streaming</a></li>
        <li><a href="#ruby-llm">RubyLLM</a></li>
        <li><a href="#devise">Devise</a></li>
        <li><a href="#git">Git</a></li>
        <li><a href="#product-design">Product Design</a></li>
    </ul>
</nav>

<main>

    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!-- SECTION: ACTIVE RECORD & DATABASE -->
    <!-- ═══════════════════════════════════════════════════════════════ -->

    <h2 class="section-title"><span class="icon">💾</span> Active Record & Database</h2>

    <!-- FICHE 1: ACTIVE RECORD BASICS -->
    <section id="active-record" class="card">
        <h2><span class="emoji">💎</span> Active Record Basics</h2>
        <div class="tags">
            <span class="tag rails">Rails</span>
            <span class="tag ruby">Ruby</span>
        </div>

        <h3>📌 Points clés</h3>
        <ul>
            <li><strong>Active Record</strong> = ORM (Object-Relational Mapping) de Rails</li>
            <li><strong>1 objet Ruby = 1 ligne</strong> dans la base de données</li>
            <li>Convention : table <code>restaurants</code> (pluriel) → classe <code>Restaurant</code> (singulier)</li>
            <li>Le modèle hérite de <code>ActiveRecord::Base</code> (ou <code>ApplicationRecord</code>)</li>
            <li><code>save</code> retourne <code>true/false</code> | <code>save!</code> lève une exception si échec</li>
            <li><code>Restaurant.all</code> retourne une <code>ActiveRecord::Relation</code> (lazy loading)</li>
            <li><code>Restaurant.count</code> est plus rapide que <code>Restaurant.all.length</code> (SQL COUNT)</li>
        </ul>

        <h3>📊 CRUD avec Active Record</h3>
        <div class="table-wrapper">
            <table>
                <tr>
                    <th>Action</th>
                    <th>Ruby</th>
                    <th>SQL généré</th>
                </tr>
                <tr>
                    <td><strong>Create</strong></td>
                    <td><code>Restaurant.new(...).save</code></td>
                    <td>INSERT INTO restaurants...</td>
                </tr>
                <tr>
                    <td><strong>Read all</strong></td>
                    <td><code>Restaurant.all</code></td>
                    <td>SELECT * FROM restaurants</td>
                </tr>
                <tr>
                    <td><strong>Read one</strong></td>
                    <td><code>Restaurant.find(1)</code></td>
                    <td>SELECT * WHERE id = 1</td>
                </tr>
                <tr>
                    <td><strong>Read filter</strong></td>
                    <td><code>Restaurant.where(rating: 5)</code></td>
                    <td>SELECT * WHERE rating = 5</td>
                </tr>
                <tr>
                    <td><strong>Update</strong></td>
                    <td><code>resto.name = "x"; resto.save</code></td>
                    <td>UPDATE restaurants SET...</td>
                </tr>
                <tr>
                    <td><strong>Delete</strong></td>
                    <td><code>resto.destroy</code></td>
                    <td>DELETE FROM restaurants WHERE...</td>
                </tr>
            </table>
        </div>

        <h3>🔍 Méthodes de recherche</h3>
        <div class="table-wrapper">
            <table>
                <tr>
                    <th>Méthode</th>
                    <th>Retourne</th>
                    <th>Usage</th>
                </tr>
                <tr>
                    <td><code>find(id)</code></td>
                    <td>1 instance ou erreur</td>
                    <td>Recherche par ID</td>
                </tr>
                <tr>
                    <td><code>find_by(attr: val)</code></td>
                    <td>1 instance ou nil</td>
                    <td>Premier match par attribut</td>
                </tr>
                <tr>
                    <td><code>where(attr: val)</code></td>
                    <td>Relation (plusieurs)</td>
                    <td>Tous les matchs</td>
                </tr>
                <tr>
                    <td><code>first</code> / <code>last</code></td>
                    <td>1 instance ou nil</td>
                    <td>Premier/dernier enregistrement</td>
                </tr>
            </table>
        </div>

        <h3>⚠️ Erreurs fréquentes</h3>
        <ul class="errors">
            <li>Oublier <code>.save</code> après <code>.new</code> → l'objet n'est pas persisté</li>
            <li>Confondre <code>find</code> (erreur si pas trouvé) et <code>find_by</code> (nil si pas trouvé)</li>
            <li>Utiliser <code>all.length</code> au lieu de <code>count</code> → charge tout en mémoire</li>
            <li>Oublier que <code>where</code> retourne une Relation, pas une instance unique</li>
        </ul>

        <h3>💻 Exemples de code</h3>
<pre><code><span class="comment"># Création et sauvegarde</span>
resto = <span class="class">Restaurant</span>.<span class="method">new</span>(name: <span class="string">"Chez Paul"</span>, rating: <span class="number">5</span>)
resto.<span class="method">save</span>  <span class="comment"># INSERT INTO restaurants...</span>

<span class="comment"># Recherche avec LIKE</span>
<span class="class">Restaurant</span>.<span class="method">where</span>(<span class="string">"name LIKE ?"</span>, <span class="string">"%pizza%"</span>)

<span class="comment"># Chaînage de méthodes</span>
<span class="class">Restaurant</span>.<span class="method">where</span>(city: <span class="string">"Paris"</span>).<span class="method">where</span>(<span class="string">"rating >= ?"</span>, <span class="number">4</span>).<span class="method">order</span>(<span class="symbol">:name</span>)</code></pre>
    </section>

    <!-- FICHE 2: MIGRATIONS -->
    <section id="migrations" class="card">
        <h2><span class="emoji">🔄</span> Migrations</h2>
        <div class="tags">
            <span class="tag rails">Rails</span>
        </div>

        <h3>📌 Points clés</h3>
        <ul>
            <li><strong>Migration</strong> = fichier Ruby pour modifier la STRUCTURE de la BDD (pas les données)</li>
            <li>Nommage : <code>TIMESTAMP_action_description.rb</code></li>
            <li><code>rails db:migrate</code> exécute les migrations non jouées</li>
            <li><code>t.timestamps</code> ajoute automatiquement <code>created_at</code> et <code>updated_at</code></li>
            <li><code>t.references :model, foreign_key: true</code> pour les clés étrangères</li>
            <li>Ne JAMAIS modifier une migration déjà exécutée → en créer une nouvelle</li>
        </ul>

        <h3>📊 Commandes Rake</h3>
        <div class="table-wrapper">
            <table>
                <tr>
                    <th>Commande</th>
                    <th>Action</th>
                </tr>
                <tr>
                    <td><code>rails db:create</code></td>
                    <td>Crée la base de données</td>
                </tr>
                <tr>
                    <td><code>rails db:drop</code></td>
                    <td>Supprime la BDD ⚠️ perte de données</td>
                </tr>
                <tr>
                    <td><code>rails db:migrate</code></td>
                    <td>Exécute les migrations pending</td>
                </tr>
                <tr>
                    <td><code>rails db:rollback</code></td>
                    <td>Annule la dernière migration</td>
                </tr>
                <tr>
                    <td><code>rails db:seed</code></td>
                    <td>Lance le fichier db/seeds.rb</td>
                </tr>
                <tr>
                    <td><code>rails console</code></td>
                    <td>Console interactive Rails</td>
                </tr>
            </table>
        </div>

        <h3>⚠️ Erreurs fréquentes</h3>
        <ul class="errors">
            <li>Oublier <code>foreign_key: true</code> sur les références</li>
            <li>Modifier une migration déjà exécutée au lieu d'en créer une nouvelle</li>
            <li>Oublier <code>t.timestamps</code> dans create_table</li>
            <li>Ne pas lancer <code>rails db:migrate</code> après création</li>
        </ul>

        <h3>💻 Exemples de code</h3>
<pre><code><span class="comment"># Migration CreateRestaurants</span>
<span class="keyword">class</span> <span class="class">CreateRestaurants</span> < ActiveRecord::Migration[<span class="number">7.1</span>]
  <span class="keyword">def</span> <span class="method">change</span>
    create_table <span class="symbol">:restaurants</span> <span class="keyword">do</span> |t|
      t.string <span class="symbol">:name</span>, null: <span class="keyword">false</span>
      t.string <span class="symbol">:address</span>
      t.integer <span class="symbol">:rating</span>
      t.references <span class="symbol">:city</span>, foreign_key: <span class="keyword">true</span>
      t.timestamps
    <span class="keyword">end</span>
  <span class="keyword">end</span>
<span class="keyword">end</span>

<span class="comment"># Seeds avec Faker</span>
<span class="number">100</span>.times <span class="keyword">do</span>
  <span class="class">Restaurant</span>.<span class="method">create!</span>(
    name: <span class="class">Faker</span>::<span class="class">Restaurant</span>.name,
    address: <span class="class">Faker</span>::<span class="class">Address</span>.street_address,
    rating: rand(<span class="number">1</span>..<span class="number">5</span>)
  )
<span class="keyword">end</span></code></pre>
    </section>

    <!-- FICHE 3: ASSOCIATIONS -->
    <section id="associations" class="card">
        <h2><span class="emoji">🔗</span> Associations</h2>
        <div class="tags">
            <span class="tag rails">Rails</span>
        </div>

        <h3>📌 Points clés</h3>
        <ul>
            <li>Relation <strong>1:N</strong> → <code>has_many</code> côté "1", <code>belongs_to</code> côté "N"</li>
            <li>La <strong>foreign key</strong> est toujours dans la table qui <code>belongs_to</code></li>
            <li><code>belongs_to</code> = <strong>singulier</strong> | <code>has_many</code> = <strong>pluriel</strong></li>
            <li><code>through:</code> permet de traverser une table de jointure (N:N)</li>
            <li>Toujours déclarer l'association des <strong>DEUX côtés</strong></li>
            <li>Assignation directe possible : <code>intern.doctor = doctor</code></li>
        </ul>

        <h3>📊 Types d'associations</h3>
        <div class="table-wrapper">
            <table>
                <tr>
                    <th>Type</th>
                    <th>Côté 1</th>
                    <th>Côté N</th>
                    <th>FK dans</th>
                </tr>
                <tr>
                    <td><strong>1:N</strong></td>
                    <td><code>has_many :interns</code></td>
                    <td><code>belongs_to :doctor</code></td>
                    <td>interns (doctor_id)</td>
                </tr>
                <tr>
                    <td><strong>N:N</strong></td>
                    <td><code>has_many :consultations</code><br><code>has_many :patients, through:</code></td>
                    <td><code>belongs_to :doctor</code><br><code>belongs_to :patient</code></td>
                    <td>consultations (2 FK)</td>
                </tr>
                <tr>
                    <td><strong>1:1</strong></td>
                    <td><code>has_one :profile</code></td>
                    <td><code>belongs_to :user</code></td>
                    <td>profiles (user_id)</td>
                </tr>
            </table>
        </div>

        <h3>⚠️ Erreurs fréquentes</h3>
        <ul class="errors">
            <li>Oublier de déclarer l'association des DEUX côtés</li>
            <li>FK dans la mauvaise table (toujours côté belongs_to)</li>
            <li>Confondre singulier/pluriel dans les noms d'association</li>
            <li>Oublier <code>has_many :consultations</code> avant le <code>through:</code></li>
            <li>Pas de migration pour créer la colonne FK</li>
        </ul>

        <h3>💻 Exemples de code</h3>
<pre><code><span class="comment"># Modèles avec associations</span>
<span class="keyword">class</span> <span class="class">Doctor</span> < ApplicationRecord
  has_many <span class="symbol">:interns</span>
  has_many <span class="symbol">:consultations</span>
  has_many <span class="symbol">:patients</span>, through: <span class="symbol">:consultations</span>
<span class="keyword">end</span>

<span class="keyword">class</span> <span class="class">Intern</span> < ApplicationRecord
  belongs_to <span class="symbol">:doctor</span>
<span class="keyword">end</span>

<span class="keyword">class</span> <span class="class">Consultation</span> < ApplicationRecord
  belongs_to <span class="symbol">:doctor</span>
  belongs_to <span class="symbol">:patient</span>
<span class="keyword">end</span>

<span class="comment"># Utilisation</span>
doctor.interns         <span class="comment"># tous les internes</span>
doctor.patients        <span class="comment"># via through</span>
intern.doctor = doctor <span class="comment"># assignation directe</span></code></pre>
    </section>

    <!-- FICHE 4: VALIDATIONS -->
    <section id="validations" class="card">
        <h2><span class="emoji">✅</span> Validations</h2>
        <div class="tags">
            <span class="tag rails">Rails</span>
        </div>

        <h3>📌 Points clés</h3>
        <ul>
            <li>Les validations vérifient les données <strong>AVANT</strong> insertion en BDD</li>
            <li><code>valid?</code> retourne <code>true/false</code></li>
            <li><code>errors.messages</code> retourne un hash avec les erreurs</li>
            <li><code>save</code> retourne <code>false</code> si invalide (pas d'insertion)</li>
            <li><code>save!</code> lève une exception si invalide</li>
            <li>Définies dans le modèle avec <code>validates</code></li>
        </ul>

        <h3>📊 Types de validations</h3>
        <div class="table-wrapper">
            <table>
                <tr>
                    <th>Type</th>
                    <th>Syntaxe</th>
                    <th>Description</th>
                </tr>
                <tr>
                    <td><strong>presence</strong></td>
                    <td><code>validates :name, presence: true</code></td>
                    <td>Champ obligatoire</td>
                </tr>
                <tr>
                    <td><strong>uniqueness</strong></td>
                    <td><code>validates :email, uniqueness: true</code></td>
                    <td>Valeur unique</td>
                </tr>
                <tr>
                    <td><strong>uniqueness scoped</strong></td>
                    <td><code>uniqueness: { scope: :city }</code></td>
                    <td>Unique dans un contexte</td>
                </tr>
                <tr>
                    <td><strong>length</strong></td>
                    <td><code>validates :name, length: { minimum: 3 }</code></td>
                    <td>Contrainte de longueur</td>
                </tr>
                <tr>
                    <td><strong>format</strong></td>
                    <td><code>validates :email, format: { with: /regex/ }</code></td>
                    <td>Pattern regex</td>
                </tr>
                <tr>
                    <td><strong>numericality</strong></td>
                    <td><code>validates :age, numericality: { only_integer: true }</code></td>
                    <td>Valeur numérique</td>
                </tr>
                <tr>
                    <td><strong>inclusion</strong></td>
                    <td><code>validates :status, inclusion: { in: %w[active inactive] }</code></td>
                    <td>Dans une liste</td>
                </tr>
            </table>
        </div>

        <h3>⚠️ Erreurs fréquentes</h3>
        <ul class="errors">
            <li>Utiliser <code>save</code> sans vérifier le retour (false = échec silencieux)</li>
            <li>Confondre <code>save</code> et <code>save!</code></li>
            <li><code>uniqueness</code> ne garantit pas 100% (race condition possible)</li>
            <li>Ne pas tester <code>valid?</code> avant de déboguer</li>
        </ul>

        <h3>💻 Exemples de code</h3>
<pre><code><span class="keyword">class</span> <span class="class">Doctor</span> < ApplicationRecord
  validates <span class="symbol">:first_name</span>, presence: <span class="keyword">true</span>
  validates <span class="symbol">:last_name</span>, presence: <span class="keyword">true</span>
  validates <span class="symbol">:email</span>, format: { with: <span class="string">/\A[^@]+@[^@]+\z/</span> }
  validates <span class="symbol">:specialty</span>, uniqueness: { scope: <span class="symbol">:city</span> }
<span class="keyword">end</span>

<span class="comment"># Test validation</span>
doctor = <span class="class">Doctor</span>.<span class="method">new</span>(first_name: <span class="string">""</span>)
doctor.<span class="method">valid?</span>          <span class="comment"># => false</span>
doctor.errors.messages <span class="comment"># => { first_name: ["can't be blank"] }</span></code></pre>
    </section>

    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!-- SECTION: IA & LLM -->
    <!-- ═══════════════════════════════════════════════════════════════ -->

    <h2 class="section-title"><span class="icon">🤖</span> Intelligence Artificielle & LLM</h2>

    <!-- Les autres fiches suivent le même pattern... -->
    <!-- Je vais en ajouter quelques-unes pour montrer, mais le code est trop long pour tout inclure ici -->

    <!-- FICHE 5: LLM BASICS -->
    <section id="llm-basics" class="card">
        <h2><span class="emoji">🧠</span> LLM Basics</h2>
        <div class="tags">
            <span class="tag ai">AI</span>
        </div>

        <h3>📌 Points clés</h3>
        <ul>
            <li><strong>LLM</strong> = Large Language Model, entraîné sur du texte massif pour prédire des mots</li>
            <li>Utiliser <code>ruby_llm</code> gem pour intégrer les LLMs dans Rails</li>
            <li><strong>Tokens</strong> = unités de base traitées par le LLM (≈ 4 caractères)</li>
            <li><strong>Context window</strong> = nombre max de tokens par requête</li>
            <li><strong>Temperature</strong> = contrôle la créativité (0 = déterministe, 1 = créatif)</li>
            <li>Toujours protéger les API keys avec <code>.env</code> et dotenv</li>
        </ul>
    </section>

    <!-- Continue avec les autres sections... -->
        <h3>📊 Concepts clés</h3>
                <div class="table-wrapper">
                    <table>
                        <tr>
                            <th>Concept</th>
                            <th>Description</th>
                            <th>Exemple</th>
                        </tr>
                        <tr>
                            <td><strong>Token</strong></td>
                            <td>Unité de facturation</td>
                            <td>~4 chars = 1 token</td>
                        </tr>
                        <tr>
                            <td><strong>Context Window</strong></td>
                            <td>Limite input+output</td>
                            <td>128K tokens (GPT-4)</td>
                        </tr>
                        <tr>
                            <td><strong>Temperature</strong></td>
                            <td>Randomness / créativité</td>
                            <td>0.0 → 1.0</td>
                        </tr>
                    </table>
                </div>

                <h3>⚠️ Erreurs fréquentes</h3>
                <ul class="errors">
                    <li>Commiter l'API key dans le repo Git</li>
                    <li>Oublier de définir un budget pour limiter les coûts</li>
                    <li>Ne pas protéger les routes LLM derrière une authentification</li>
                </ul>

                <h3>💻 Exemples de code</h3>
        <pre><code><span class="comment"># Setup RubyLLM</span>
        <span class="class">RubyLLM</span>.configure <span class="keyword">do</span> |config|
          config.openai_api_key = <span class="class">ENV</span>[<span class="string">"OPENAI_API_KEY"</span>]
        <span class="keyword">end</span>

        <span class="comment"># Basic chat</span>
        chat = <span class="class">RubyLLM</span>.chat
        response = chat.<span class="method">ask</span>(<span class="string">"What is Ruby on Rails?"</span>)
        puts response.content</code></pre>
            </section>

            <!-- FICHE 6: PROMPT ENGINEERING -->
            <section id="prompt-engineering" class="card purple">
                <h2><span class="emoji">✍️</span> Prompt Engineering</h2>
                <div class="tags">
                    <span class="tag ai">AI</span>
                </div>

                <h3>📌 Points clés</h3>
                <ul>
                    <li>Structurer le prompt : <strong>Persona → Context → Task → Format</strong></li>
                    <li><strong>Persona</strong> : définir QUI est l'assistant</li>
                    <li><strong>Context</strong> : donner des infos sur l'utilisateur et l'usage</li>
                    <li><strong>Task</strong> : objectif clair et spécifique</li>
                    <li><strong>Format</strong> : préciser le format de sortie (Markdown, JSON...)</li>
                    <li><strong>Few-shot prompting</strong> : donner des exemples input/output</li>
                </ul>

                <h3>📊 Structure d'un bon prompt</h3>
                <div class="table-wrapper">
                    <table>
                        <tr>
                            <th>Élément</th>
                            <th>Exemple</th>
                        </tr>
                        <tr>
                            <td><strong>Persona</strong></td>
                            <td><code>You are an experienced chef...</code></td>
                        </tr>
                        <tr>
                            <td><strong>Context</strong></td>
                            <td><code>I am a beginner cook...</code></td>
                        </tr>
                        <tr>
                            <td><strong>Task</strong></td>
                            <td><code>Guide me into making...</code></td>
                        </tr>
                        <tr>
                            <td><strong>Format</strong></td>
                            <td><code>Answer in Markdown bullets</code></td>
                        </tr>
                    </table>
                </div>

                <h3>⚠️ Erreurs fréquentes</h3>
                <ul class="errors">
                    <li>Prompts vagues : "What do you think?" → Être spécifique</li>
                    <li>Formulation négative : "Don't give code" → Préférer "Explain step-by-step, no code"</li>
                    <li>Surcharger un seul prompt → Découper en étapes</li>
                    <li>Oublier le contexte ou le rôle</li>
                </ul>

                <h3>💻 Exemples de code</h3>
        <pre><code><span class="class">SYSTEM_PROMPT</span> = <span class="string"><<~PROMPT
          You are a Teaching Assistant.
          I am a student learning to code.
          Help me break down problems without giving solutions.
          Answer concisely in Markdown.
        PROMPT</span>

        ruby_llm_chat.<span class="method">with_instructions</span>(<span class="class">SYSTEM_PROMPT</span>)
        response = ruby_llm_chat.<span class="method">ask</span>(user_question)</code></pre>
            </section>

            <!-- FICHE 7: CHAT MODELS -->
            <section id="chat-models" class="card cyan">
                <h2><span class="emoji">💬</span> Chat Models (Rails + AI)</h2>
                <div class="tags">
                    <span class="tag rails">Rails</span>
                    <span class="tag ai">AI</span>
                </div>

                <h3>📌 Points clés</h3>
                <ul>
                    <li>Structure : <code>User → Chat → Message</code></li>
                    <li>Message a un <code>role</code> : "user" ou "assistant"</li>
                    <li>Stocker le <strong>system_prompt</strong> par entité (Challenge, etc.)</li>
                    <li>Générer automatiquement le titre du chat via LLM</li>
                    <li>Utiliser <code>with_instructions()</code> pour le system prompt</li>
                </ul>

                <h3>📊 Architecture des modèles</h3>
                <div class="table-wrapper">
                    <table>
                        <tr>
                            <th>Model</th>
                            <th>Attributs clés</th>
                            <th>Associations</th>
                        </tr>
                        <tr>
                            <td><strong>Chat</strong></td>
                            <td>title, challenge_id, user_id</td>
                            <td>belongs_to :user, :challenge<br>has_many :messages</td>
                        </tr>
                        <tr>
                            <td><strong>Message</strong></td>
                            <td>role, content, chat_id</td>
                            <td>belongs_to :chat</td>
                        </tr>
                    </table>
                </div>

                <h3>⚠️ Erreurs fréquentes</h3>
                <ul class="errors">
                    <li>Ne pas passer le context (challenge.content) au LLM</li>
                    <li>Oublier de sauvegarder la réponse assistant en DB</li>
                    <li>Ne pas scoper les chats par <code>current_user</code></li>
                </ul>

                <h3>💻 Exemples de code</h3>
        <pre><code><span class="comment"># Controller create message</span>
        <span class="variable">@message</span> = <span class="class">Message</span>.<span class="method">new</span>(content: params[<span class="symbol">:content</span>], role: <span class="string">"user"</span>, chat: <span class="variable">@chat</span>)
        <span class="keyword">if</span> <span class="variable">@message</span>.<span class="method">save</span>
          response = ruby_llm_chat
            .<span class="method">with_instructions</span>(instructions)
            .<span class="method">ask</span>(<span class="variable">@message</span>.content)
          <span class="class">Message</span>.<span class="method">create</span>(role: <span class="string">"assistant"</span>, content: response.content, chat: <span class="variable">@chat</span>)
        <span class="keyword">end</span></code></pre>
            </section>

            <!-- FICHE 8: TOOLS & AGENTS -->
            <section id="tools-agents" class="card cyan">
                <h2><span class="emoji">🔧</span> Tools & Agents</h2>
                <div class="tags">
                    <span class="tag ai">AI</span>
                    <span class="tag ruby">Ruby</span>
                </div>

                <h3>📌 Points clés</h3>
                <ul>
                    <li><strong>Tools</strong> = permettent au LLM d'appeler du code Ruby (APIs, DB...)</li>
                    <li>Hériter de <code>RubyLLM::Tool</code> avec <code>description</code> et <code>param</code></li>
                    <li>Méthode <code>execute</code> contient la logique métier</li>
                    <li>Ajouter le tool avec <code>.with_tool(MyTool.new)</code></li>
                    <li><strong>Workflow</strong> = ordre fixe décidé par le dev</li>
                    <li><strong>Agent</strong> = LLM choisit quel(s) tool(s) utiliser</li>
                </ul>

                <h3>📊 Workflow vs Agent</h3>
                <div class="table-wrapper">
                    <table>
                        <tr>
                            <th>Concept</th>
                            <th>Contrôle</th>
                            <th>Use case</th>
                        </tr>
                        <tr>
                            <td><strong>Workflow</strong></td>
                            <td>Développeur</td>
                            <td>Tâches bien définies, prévisibles</td>
                        </tr>
                        <tr>
                            <td><strong>Agent</strong></td>
                            <td>LLM</td>
                            <td>Flexibilité, décisions dynamiques</td>
                        </tr>
                    </table>
                </div>

                <h3>⚠️ Erreurs fréquentes</h3>
                <ul class="errors">
                    <li>Ne pas tester le tool seul avant de l'intégrer au LLM</li>
                    <li>Description de tool trop vague → le LLM ne sait pas quand l'utiliser</li>
                    <li>Oublier de gérer les erreurs (rescue)</li>
                </ul>

                <h3>💻 Exemples de code</h3>
        <pre><code><span class="comment"># app/tools/weather_tool.rb</span>
        <span class="keyword">class</span> <span class="class">WeatherTool</span> < RubyLLM::Tool
          description <span class="string">"Gets current weather for a location"</span>
          param <span class="symbol">:latitude</span>, desc: <span class="string">"Latitude (e.g., 52.5200)"</span>
          param <span class="symbol">:longitude</span>, desc: <span class="string">"Longitude (e.g., 13.4050)"</span>

          <span class="keyword">def</span> <span class="method">execute</span>(latitude:, longitude:)
            url = <span class="string">"https://api.open-meteo.com/v1/forecast?latitude=</span>#{latitude}<span class="string">&longitude=</span>#{longitude}<span class="string">"</span>
            <span class="class">JSON</span>.parse(<span class="class">URI</span>.parse(url).read)
          <span class="keyword">rescue</span> => e
            { error: e.message }
          <span class="keyword">end</span>
        <span class="keyword">end</span>

        <span class="comment"># Usage</span>
        ruby_llm_chat.<span class="method">with_tool</span>(<span class="class">WeatherTool</span>.<span class="method">new</span>).<span class="method">ask</span>(<span class="string">"Weather in Paris?"</span>)</code></pre>
            </section>

            <!-- FICHE 9: STREAMING & WEBSOCKETS -->
            <section id="streaming" class="card cyan">
                <h2><span class="emoji">📡</span> Streaming & WebSockets</h2>
                <div class="tags">
                    <span class="tag rails">Rails</span>
                    <span class="tag js">JS</span>
                </div>

                <h3>📌 Points clés</h3>
                <ul>
                    <li><strong>HTTP</strong> = request/response unique, attente complète</li>
                    <li><strong>WebSocket</strong> = connexion bidirectionnelle persistante</li>
                    <li><strong>Action Cable</strong> = lib Rails pour WebSockets + Turbo Streams</li>
                    <li>Utiliser <code>solid_cable</code> gem avec PostgreSQL (ou Redis)</li>
                    <li>Stream LLM : passer un <strong>block</strong> à <code>chat.ask</code></li>
                    <li><code>broadcast_replace_to</code> pour mettre à jour le DOM en temps réel</li>
                </ul>

                <h3>📊 Étapes du streaming</h3>
                <div class="table-wrapper">
                    <table>
                        <tr>
                            <th>Étape</th>
                            <th>Action</th>
                        </tr>
                        <tr>
                            <td>1. User message</td>
                            <td><code>broadcast_append_to</code> → afficher message</td>
                        </tr>
                        <tr>
                            <td>2. Assistant (vide)</td>
                            <td>Créer message vide, append "..."</td>
                        </tr>
                        <tr>
                            <td>3. Chunks LLM</td>
                            <td><code>broadcast_replace_to</code> à chaque chunk</td>
                        </tr>
                    </table>
                </div>

                <h3>⚠️ Erreurs fréquentes</h3>
                <ul class="errors">
                    <li>Oublier <code>&lt;%= turbo_stream_from @chat %&gt;</code> dans la vue</li>
                    <li>Ne pas définir <code>id="&lt;%= dom_id(message) %&gt;"</code> sur le div à remplacer</li>
                    <li>Ignorer les chunks vides (<code>next if chunk.content.blank?</code>)</li>
                </ul>

                <h3>💻 Exemples de code</h3>
        <pre><code><span class="comment"># Streaming avec block</span>
        <span class="variable">@response</span> = ruby_llm_chat.<span class="method">ask</span>(<span class="variable">@message</span>.content) <span class="keyword">do</span> |chunk|
          <span class="keyword">next if</span> chunk.content.<span class="method">blank?</span>
          <span class="variable">@assistant_message</span>.content += chunk.content
          <span class="method">broadcast_replace</span>(<span class="variable">@assistant_message</span>)
        <span class="keyword">end</span>

        <span class="comment"># View subscription</span>
        <span class="string">&lt;%= turbo_stream_from @chat %&gt;</span>

        <span class="comment"># Model callback</span>
        after_create_commit <span class="symbol">:broadcast_append_to_chat</span></code></pre>
            </section>

            <!-- FICHE 10: RUBYLLM CHEATSHEET -->
            <section id="ruby-llm" class="card purple">
                <h2><span class="emoji">💎</span> RubyLLM Cheatsheet</h2>
                <div class="tags">
                    <span class="tag ruby">Ruby</span>
                    <span class="tag ai">AI</span>
                </div>

                <h3>📌 Points clés</h3>
                <ul>
                    <li>Gem : <code>gem "ruby_llm", "~> 1.2.0"</code></li>
                    <li>Config dans <code>config/initializers/ruby_llm.rb</code></li>
                    <li>Alternative gratuite : GitHub Models (Azure OpenAI)</li>
                    <li><code>RubyLLM.paint()</code> pour générer des images</li>
                    <li>Vision : utiliser <code>gpt-4o</code> avec <code>with: { image: path }</code></li>
                </ul>

                <h3>📊 Méthodes principales</h3>
                <div class="table-wrapper">
                    <table>
                        <tr>
                            <th>Méthode</th>
                            <th>Usage</th>
                        </tr>
                        <tr>
                            <td><code>RubyLLM.chat</code></td>
                            <td>Créer une instance de chat</td>
                        </tr>
                        <tr>
                            <td><code>.ask(prompt)</code></td>
                            <td>Envoyer un message</td>
                        </tr>
                        <tr>
                            <td><code>.with_instructions()</code></td>
                            <td>Définir le system prompt</td>
                        </tr>
                        <tr>
                            <td><code>.with_tool()</code></td>
                            <td>Ajouter un tool</td>
                        </tr>
                        <tr>
                            <td><code>.with_temperature()</code></td>
                            <td>Régler la créativité</td>
                        </tr>
                        <tr>
                            <td><code>response.content</code></td>
                            <td>Récupérer le texte</td>
                        </tr>
                        <tr>
                            <td><code>response.input_tokens</code></td>
                            <td>Tokens utilisés (input)</td>
                        </tr>
                    </table>
                </div>

                <h3>⚠️ Erreurs fréquentes</h3>
                <ul class="errors">
                    <li>Oublier <code>bundle install</code> après ajout de la gem</li>
                    <li>API key non définie dans <code>.env</code></li>
                    <li>Ne pas tracker les tokens pour monitorer les coûts</li>
                </ul>

                <h3>💻 Exemples de code</h3>
        <pre><code><span class="comment"># Config GitHub Models (gratuit)</span>
        <span class="class">RubyLLM</span>.configure <span class="keyword">do</span> |config|
          config.openai_api_key = <span class="class">ENV</span>[<span class="string">'GITHUB_TOKEN'</span>]
          config.openai_api_base = <span class="string">"https://models.inference.ai.azure.com"</span>
        <span class="keyword">end</span>

        <span class="comment"># Image generation</span>
        image = <span class="class">RubyLLM</span>.<span class="method">paint</span>(<span class="string">"A Le Wagon student coding"</span>)
        image.<span class="method">save</span>(<span class="string">"student.png"</span>)

        <span class="comment"># Vision</span>
        chat = <span class="class">RubyLLM</span>.<span class="method">chat</span>(model: <span class="string">'gpt-4o'</span>)
        chat.<span class="method">ask</span>(<span class="string">"Describe this image"</span>, with: { image: <span class="string">"photo.png"</span> })</code></pre>
            </section>

            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- SECTION: RAILS ESSENTIALS -->
            <!-- ═══════════════════════════════════════════════════════════════ -->

            <h2 class="section-title"><span class="icon">🔐</span> Rails Essentials</h2>

            <!-- FICHE 11: DEVISE -->
            <section id="devise" class="card ruby">
                <h2><span class="emoji">🔐</span> Devise Authentication</h2>
                <div class="tags">
                    <span class="tag rails">Rails</span>
                </div>

                <h3>📌 Points clés</h3>
                <ul>
                    <li><code>gem "devise"</code> + <code>rails generate devise:install</code></li>
                    <li><code>rails generate devise User</code> → model + migration + routes</li>
                    <li>Helpers : <code>user_signed_in?</code>, <code>current_user</code></li>
                    <li>Protéger toutes les routes : <code>before_action :authenticate_user!</code></li>
                    <li><code>skip_before_action</code> pour les pages publiques</li>
                </ul>

                <h3>📊 Routes Devise</h3>
                <div class="table-wrapper">
                    <table>
                        <tr>
                            <th>Route</th>
                            <th>Path</th>
                            <th>Action</th>
                        </tr>
                        <tr>
                            <td><code>new_user_session</code></td>
                            <td>/users/sign_in</td>
                            <td>Login</td>
                        </tr>
                        <tr>
                            <td><code>destroy_user_session</code></td>
                            <td>/users/sign_out</td>
                            <td>Logout</td>
                        </tr>
                        <tr>
                            <td><code>new_user_registration</code></td>
                            <td>/users/sign_up</td>
                            <td>Sign up</td>
                        </tr>
                    </table>
                </div>

                <h3>⚠️ Erreurs fréquentes</h3>
                <ul class="errors">
                    <li>Oublier <code>rails db:migrate</code> après génération</li>
                    <li>Ne pas configurer <code>default_url_options</code> dans environments</li>
                    <li>Oublier de sanitizer les params additionnels (first_name, etc.)</li>
                </ul>

                <h3>💻 Exemples de code</h3>
        <pre><code><span class="comment"># ApplicationController - whitelist approach</span>
        <span class="keyword">class</span> <span class="class">ApplicationController</span> < ActionController::Base
          before_action <span class="symbol">:authenticate_user!</span>
        <span class="keyword">end</span>

        <span class="comment"># Skip pour pages publiques</span>
        <span class="keyword">class</span> <span class="class">PagesController</span> < ApplicationController
          skip_before_action <span class="symbol">:authenticate_user!</span>, only: <span class="symbol">:home</span>
        <span class="keyword">end</span>

        <span class="comment"># Ajouter des champs</span>
        devise_parameter_sanitizer.<span class="method">permit</span>(<span class="symbol">:sign_up</span>, keys: [<span class="symbol">:first_name</span>])</code></pre>
            </section>

            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- SECTION: WORKFLOW & MÉTHODOLOGIE -->
            <!-- ═══════════════════════════════════════════════════════════════ -->

            <h2 class="section-title"><span class="icon">🌿</span> Workflow & Méthodologie</h2>

            <!-- FICHE 12: GIT -->
            <section id="git" class="card yellow">
                <h2><span class="emoji">🌿</span> Git & GitHub Collaboration</h2>
                <div class="tags">
                    <span class="tag git">Git</span>
                </div>

                <h3>📌 Points clés</h3>
                <ul>
                    <li><strong>1 feature = 1 branch</strong></li>
                    <li>Ne JAMAIS commit directement sur <code>master</code></li>
                    <li>Toujours avoir un <code>git status</code> clean avant pull/checkout/merge</li>
                    <li><strong>Pull Request</strong> = code review + merge</li>
                    <li><code>closes #123</code> dans PR description ferme l'issue automatiquement</li>
                </ul>

                <h3>📊 Commandes essentielles</h3>
                <div class="table-wrapper">
                    <table>
                        <tr>
                            <th>Commande</th>
                            <th>Action</th>
                        </tr>
                        <tr>
                            <td><code>git checkout -b feature-name</code></td>
                            <td>Créer et switch sur nouvelle branch</td>
                        </tr>
                        <tr>
                            <td><code>git push origin feature-name</code></td>
                            <td>Push la branch sur GitHub</td>
                        </tr>
                        <tr>
                            <td><code>git pull origin master</code></td>
                            <td>Récupérer le master à jour</td>
                        </tr>
                        <tr>
                            <td><code>git merge master</code></td>
                            <td>Merger master dans sa branch</td>
                        </tr>
                    </table>
                </div>

                <h3>⚠️ Erreurs fréquentes</h3>
                <ul class="errors">
                    <li>Pull/merge avec un <code>git status</code> pas clean → conflits</li>
                    <li>Oublier <code>bundle install</code> et <code>rails db:migrate</code> après pull</li>
                    <li>Ne pas communiquer avec l'équipe sur les fichiers modifiés</li>
                </ul>

                <h3>💻 Workflow complet</h3>
        <pre><code><span class="comment"># Workflow complet</span>
        git checkout -b add-chat-feature
        <span class="comment"># ... code ...</span>
        git add .
        git commit -m <span class="string">"Add chat functionality"</span>
        git push origin add-chat-feature
        <span class="comment"># → Create Pull Request on GitHub</span>
        <span class="comment"># → After merge:</span>
        git checkout master
        git pull origin master
        git sweep  <span class="comment"># Clean local branches</span></code></pre>
            </section>

            <!-- FICHE 13: PRODUCT DESIGN -->
            <section id="product-design" class="card yellow">
                <h2><span class="emoji">🎨</span> Product Design Sprint</h2>
                <div class="tags">
                    <span class="tag ux">UX</span>
                </div>

                <h3>📌 Points clés</h3>
                <ul>
                    <li><strong>Product Pitch</strong> = Need + Target + Goal + Differentiator</li>
                    <li><strong>User Journey</strong> = toutes les étapes pour compléter une tâche</li>
                    <li><strong>Core User Journey</strong> = le parcours principal (valeur clé)</li>
                    <li><strong>User Flow</strong> = représentation visuelle des pages et liens</li>
                    <li><strong>Wireframe</strong> = maquette lo-fi de chaque page</li>
                    <li><strong>Prototype</strong> = wireframes connectés pour démo live</li>
                </ul>

                <h3>📊 Structure du Pitch</h3>
                <div class="table-wrapper">
                    <table>
                        <tr>
                            <th>Élément</th>
                            <th>Question</th>
                            <th>Exemple (Airbnb)</th>
                        </tr>
                        <tr>
                            <td><strong>Need</strong></td>
                            <td>Action à faire</td>
                            <td>Find a place to stay</td>
                        </tr>
                        <tr>
                            <td><strong>Target</strong></td>
                            <td>Pour qui ?</td>
                            <td>Travelers seeking unique stays</td>
                        </tr>
                        <tr>
                            <td><strong>Goal</strong></td>
                            <td>Objectif futur</td>
                            <td>Live unique experiences</td>
                        </tr>
                        <tr>
                            <td><strong>Differentiator</strong></td>
                            <td>Pourquoi vous ?</td>
                            <td>Local hosts, authentic homes</td>
                        </tr>
                    </table>
                </div>

                <h3>⚠️ Erreurs fréquentes</h3>
                <ul class="errors">
                    <li>Target trop large → segmenter par persona</li>
                    <li>Confondre Need (action immédiate) et Goal (résultat espéré)</li>
                    <li>Sauter le wireframe et coder directement</li>
                    <li>UI Kit sans composants réutilisables</li>
                </ul>

                <h3>📝 Template Pitch</h3>
        <pre><code>It's a product...
          <span class="keyword">FOR</span>    [user segment]
          <span class="keyword">WHO NEED TO</span>    [perform this action]
          <span class="keyword">AND WANT TO</span>    [achieve this goal]
          <span class="keyword">THEY CHOOSE</span>    [your product]
          <span class="keyword">BECAUSE</span>    [it has this differentiator]</code></pre>
            </section>


</main>

<footer>
    <p>📚 Fiches de révision créées pour <strong>Le Wagon - Bordeaux</strong></p>
    <p>Ruby on Rails + Intelligence Artificielle | 2025</p>
</footer>

<!-- Bouton scroll to top -->
<div class="scroll-to-top" onclick="window.scrollTo({top: 0, behavior: 'smooth'})">
    ↑
</div>

<script>
// Show/hide scroll to top button
window.addEventListener('scroll', function() {
    const scrollBtn = document.querySelector('.scroll-to-top');
    if (window.scrollY > 500) {
        scrollBtn.classList.add('visible');
    } else {
        scrollBtn.classList.remove('visible');
    }
});
</script>

</body>
</html>
