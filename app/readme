<div class="row justify-content-center">
  <div class="col-md-10">
    <div class="row g-4">
      <% @blocs.each do |bloc| %>
        <div class="col-md-6">
          <%= button_to "New Chat", bloc_chats_path(bloc), class: "btn-grad" %>
            <h3><%= bloc.name %></h3>
            <p class="mb-0">Associations & Validations</p>
        </div>
      <% end %>
    </div>
  </div>
</div>

_____________________________________________________________________________

Show :


<div class="row">
  <div class="col">
    <div class="col-md-3">
      <h1><%= @chat.bloc.name %></h1>
    </div>
  </div>

  <h1 id="chat_title"><%= @chat.generate_title_from_first_message %></h1>

<div id="messages">
  <% @chat.messages.each do |message| %>
    <% if message.role == "user" %>
      <div class="d-flex justify-content-end mb-2">
        <div class="bg-white px-3 pb-0 pt-3 rounded-pill">
          <%= raw(message.content) %>
        </div>
      </div>
    <% elsif message.role == "assistant" %>
      <div class="d-flex justify-content-start mb-5">
        <div class="px-3 pb-0 pt-3">
          <%= raw(message.content) %>
        </div>
      </div>
    <% end %>
  <% end %>
</div>

  <div id="new_message_container">
    <%= simple_form_for [@chat, @message] do |f| %>
      <%= f.input :content, label: "Ask a question", required: true, placeholder: "How should I get started with the bloc?" %>
      <%= f.button :submit, 'Ask LLM', class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>

